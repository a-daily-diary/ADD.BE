version: '3.5'

services:
  nest:
    container_name: nest
    build:
      context: .
    env_file:
      - ./.env
    ports:
      - 5000:5000
    networks:
      - add_net
    depends_on:
      - postgres
    volumes:
      - ./:/app/
  postgres:
    container_name: postgres
    image: postgres:9.6.23
    restart: always
    env_file:
      - ./.env
    ports:
      - 5432:5432
    networks:
      - add_net
    # volumes:
    #   - ./db_data:/var/lib/postgresql/data
networks:
  add_net:
    driver: bridge
